# 多账号内容生产平台术语表

## 平台

管理多个内容账号的生产、审核和交付包生成的控制系统。

## 账号

平台中的隔离租户。账号拥有独立的定位、规则和素材边界；外部连接归 Owner 工作区而非账号。

## 所有者

平台的唯一审批人，也是登录控制台的身份。不要用“账号”指代所有者。

## 所有者设置

所有者管理登录密码、本机界面偏好和全局外部连接；连接由账号蓝图中相应生产能力的配置入口创建、验证、选择或轮换，而不是单独的连接页面。不要称为“账号设置”，以免与内容账号混淆。

## 操作草稿

所有者为某个 Episode 填写但尚未提交的审核内容。操作草稿不改变 Episode 状态，也不产生审批或审计记录。

## 账号蓝图

初始化或配置账号的版本化规则集合。包括账号定位、当前五项可选生产能力（静态视觉 / 图片生成、A-roll、B-roll、旁白和配乐 / 音效）声明、每项能力的执行路径及其非秘密模型或预设选择，以及审核关卡。

## 当前蓝图

账号当前可编辑的蓝图。变更后供新 Episode 使用；若已有 Episode 引用原蓝图，系统先保留其快照而不改写历史生产单。

## 凭据引用

账号蓝图为外部执行路径选择的连接版本 ID，是业务数据库中的稳定非秘密标识。服务端在蓝图保存、Episode 创建和当前 Episode 配置修复时在同一事务验证其归属、兼容性和已验证状态；本地和人工执行路径没有凭据引用。

## 外部连接

Owner 命名的全局逻辑连接，在 Owner 工作区内名称唯一并由不可变 ID 标识，可被多个账号复用。显示名称和说明可原地修改；一条连接只兼容一个已注册的生产能力、Provider 和 Adapter，首期只使用该 Adapter 的官方 Endpoint，认证材料由 Owner 经一次性受保护写入路径交给 Worker 的秘密存储，页面永不回显原值。

## 已注册 Adapter

随 Worker 部署提供并通过当前就绪探测的执行器，声明其支持的生产能力、执行位置、外部认证方式（如有）和可选模型或预设目录。Owner 只能为已注册 Adapter 创建外部连接，不能在蓝图中自行登记执行器、插件、Endpoint 或不兼容的认证材料；未部署或未就绪的本地工具不属于可选路径。

## 执行路径

账号蓝图为某项生产能力选定的生产方式，由一个已注册 Adapter、非秘密模型或预设及任务粒度组成；外部路径还须引用一条已验证连接，本地路径须由当前 Worker 报告可用，人工素材路径生成素材清单但不生成外部执行任务。首次启用能力不自动推断或选择执行路径，Owner 必须显式选择；初始 OpenChatCut A-roll 路径按分镜场景生成卡片，B-roll 路径按镜头生成卡片，旁白的声音、语速和语言由 Owner 在 Episode 镜头工作台保存本期 TTS 设置后使用。

## 人工素材清单

Episode 为启用人工素材执行路径所列出的待补齐产物，按该路径的任务粒度组织。清单中的素材由 Owner 提供并进入审核，不会由 Worker 自动生产；关闭该生产能力则不产生该清单。

## 连接版本

外部连接的一次不可变配置修订，包含生产能力、Provider、Adapter、官方 Endpoint 和不透明的秘密引用，可处于草稿、测试失败、已验证或不可用状态，由蓝图和 Episode 冻结引用。认证材料本身只在 Worker 秘密存储中；Provider、Adapter、Endpoint 或认证材料变更会新建版本，失效、撤销或轮换后的旧版本明确阻塞既有 Episode，绝不自动改用另一条连接；已被引用的版本不硬删，未被引用的草稿可永久删除。

## 已验证连接

最近一次兼容性测试通过的连接版本，才可被蓝图选择。该状态不设固定失效时间，但连接配置或秘密变更后必须重新测试；临时可重试失败保留上次已验证资格，也不能替代 Episode 生产前检查或任务执行时的真实验证。

## 连接验证结果

一次由 Owner 显式发起、针对特定连接版本的独立测试记录，确认 Adapter 的 Endpoint 可达、认证有效和最小权限，包含验证时间、脱敏结果和原因；每次结果追加保留，临时故障记录为可重试，不撤销上次已验证资格。首期不做后台健康监控，状态只在显式测试、生产前检查或任务执行时更新；它不属于 Episode 的生产前检查，也不包含 API Key、Endpoint 或服务账号秘密。

## 生产前检查

Episode 进入生产前，对已启用生产能力及其选定连接版本执行的统一前置检查。检查结果必须区分可编辑配置、可重试运行故障、连接管理和需要环境管理员处理的阻塞；模型缺失或不受 Adapter 支持归蓝图，模型被认证材料拒绝归连接，Worker 本身缺少模型能力归环境管理员；连接变为不可用时保留蓝图原选择并禁止新 Episode 通过检查。

## Episode 配置快照

Episode 创建时固定保存的生产能力声明和静态执行选择，包括所选连接版本的凭据引用；本期 TTS 设置在镜头工作台单独保存于当前 Episode，并进入每个 TTS 任务的不可变输入；它不保存凭据秘密，也不等同于连接或执行环境的当前运行状态。

## 当前 Episode 配置修复

Owner 对一个因可编辑配置受阻的 Episode 显式执行的修复动作。它可将受影响任务切换至新选定的已验证连接版本并重排这些任务，但保留其他冻结配置、已完成任务、审核和审计历史。

## 可修复目标

Worker 阻塞项明确指出可由当前 Episode 配置修复的生产能力或核心生产步骤。只有 Worker 判定为可编辑配置的问题才有可修复目标；连接、网络、凭据、注册或本机环境故障不属于可修复目标。

## 运行前置条件

生产能力在当前执行环境中可用所需的外部条件，例如注册、权限、连接和资产目录可访问性。运行前置条件不是账号蓝图中的可编辑规则。

## 生产能力

账号蓝图中可由 Owner 开启或关闭、并决定 Episode 是否生成对应任务的一类可选产能。当前目录包括静态视觉 / 图片生成、A-roll、B-roll、旁白和配乐 / 音效；任一能力若由该 Episode 的人工素材供给且不生成外部任务，就不要求外部连接。每项生产能力必须有明确的输入依据、产物、执行路径和运行前置条件；只有标签或说明文字不构成生产能力。

## 核心生产步骤

每个 Episode 都遵循的主流程，不作为蓝图能力开关：脚本输入与审核、视觉资产准备、分镜规划、渲染、QC 和发布包校验。当前脚本由 Owner 上传，不做脚本生成；视觉规划会收敛到视觉资产准备。未来若要跳过或替换其中步骤，应建模为生命周期或流程规则，而不是混入可选媒体能力目录。

## 账号硬约束

账号蓝图中不可被系列或单条 Episode 覆盖的规则，例如禁止内容和素材边界；Worker 文件读写能力与运行并发不属于账号蓝图。

## 账号默认值

账号蓝图提供的可被系列规则覆盖的默认创作偏好，例如默认语气、视觉风格或字幕样式。

## 系列

账号内可重复使用的内容格式、角色设定和叙事规则。

## 系列基线

某个 Episode 创建时固定引用的系列版本规则，作为该系列内容生成任务的统一上下文。

## Episode

一次独立、可追溯的视频生产单元。创建时固定引用一个账号蓝图版本。

## Episode 生命周期

**归档**：将不再参与默认生产列表的 Episode 隐藏，但保留其数据库记录、本地产物和审计历史。

**永久删除**：由 Owner 明确确认后，删除 Episode 及其任务、产物索引、审计记录和对应的本地 Episode 文件；这是破坏性操作，不是普通的隐藏。

## 产物

生产过程中的可验证输出，例如脚本、视觉资产、分镜、成片、发布包或截图。

## 发布包

经过 QC 并完成校验的一组最终生产产物：视频、封面、元数据和验证清单。

## 生产完成

Episode 的发布包已经生成并通过完整性校验，是平台内的最终状态。平台不记录外部发布或后续复盘。

## 审批

由 Owner 写入、附带结论和理由的状态迁移。生产 Worker 不得审批自己的产物。

## 审核修订

Owner 对当前审核包提出的、附带理由的受控变更。仅合成修订保留已批准的媒体与音轨并生成新的审核渲染；分镜结构修订返回分镜审核，不能采用当前 Studio 工程；审核阶段的镜头输入修订允许返回镜头工作台修改基础素材、口播与片段，并再次进入可编辑的 OpenChatCut 工作版本。镜头输入修订只在 Owner 再次明确生成审核视频时冻结新快照，旧审核包、产物与审计历史保持不变。这三条路径都不能就地改写既有审核证据。

## 例外

Owner 对单个 Episode 批准的一次性规则偏离。例外不会自动成为账号蓝图规则。

## Worker 任务

为 Episode 执行一个可验证生产步骤的后台任务，例如素材检查、媒体生成、渲染或发布包校验。任务只携带连接的非秘密引用；Worker 在生产前检查和每次执行时各自解析认证材料，任务拥有自己的状态、尝试次数、结果和阻塞原因。

## Worker 运行约束

Worker 任务的文件读写能力由平台按生产能力声明。有效并发取可信 Provider / 连接上限、Adapter 安全上限和 Worker 容量的最小值；没有可信 Provider 上限时使用 Adapter 的保守默认值。计算结果及来源写入任务运行快照。Worker 任务始终记录实际成本，实际成本记录不是调用前的预算保护。

## Prompt 上下文

交给生成执行器的结构化创作输入，由账号基线、系列基线、本期输入和审核反馈按任务需要组合而成；技术执行参数不属于创作上下文。

## 蓝图执行配置

账号蓝图中决定内容生产方式的非连接技术规则，包括从 Adapter 可选目录中选择的模型或卡片预设，以及 Prompt。声音和语速属于 Episode 的本期 TTS 设置。Provider、Adapter、Endpoint、认证材料、文件读写能力和运行并发由已注册 Adapter、外部连接版本或 Worker 平台确定，不由蓝图自由填写。
