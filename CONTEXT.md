# 多账号内容生产平台术语表

## 平台

管理多个内容账号的生产、审核、发布准备和复盘的控制系统。

## 账号

平台中的隔离租户。账号拥有独立的定位、规则、凭据引用、素材边界、发布记录和指标。

## 所有者

平台的唯一审批人与发布确认人，也是登录控制台的身份。不要用“账号”指代所有者。

## 所有者设置

所有者管理登录密码与本机界面偏好的入口。不要称为“账号设置”，以免与内容账号混淆。

## 操作草稿

所有者为某个 Episode 填写但尚未提交的审核或发布确认内容。操作草稿不改变 Episode 状态，也不产生审批或审计记录。

## 账号蓝图

初始化或配置账号的版本化规则集合。包括账号定位、当前五项可选生产能力（静态视觉 / 图片生成、A-roll、B-roll、旁白和配乐 / 音效）声明、允许的工具与供应商、审核关卡及发布规则。

## 凭据引用

账号蓝图按生产能力选择的一条外部连接的非秘密标识；它不代表凭据内容或连接当前是否可用。

## 外部连接

生产能力依赖的 Provider / Adapter 连接配置。连接档案归 Owner 所属工作区管理，账号蓝图按能力显式选择要使用的连接。

## 生产前检查

Episode 进入生产前，对已启用生产能力及其选定连接执行的统一前置检查。检查结果必须区分可编辑配置、可重试运行故障和需要环境管理员处理的阻塞。

## Episode 配置快照

Episode 创建时固定保存的生产能力声明和静态执行选择，包括所选凭据引用；它不保存凭据秘密，也不等同于连接或执行环境的当前运行状态。

## 运行前置条件

生产能力在当前执行环境中可用所需的外部条件，例如注册、权限、连接和资产目录可访问性。运行前置条件不是账号蓝图中的可编辑规则。

## 生产能力

账号蓝图中可由 Owner 开启或关闭、并决定 Episode 是否生成对应任务的一类可选产能。当前目录包括静态视觉 / 图片生成、A-roll、B-roll、旁白和配乐 / 音效。每项生产能力必须有明确的输入依据、产物、执行器绑定和运行前置条件；只有标签或说明文字不构成生产能力。

## 核心生产步骤

每个 Episode 都遵循的主流程，不作为蓝图能力开关：脚本生成、视觉资产准备、分镜规划、审核、渲染、QC、发布包校验和人工发布确认。视觉规划会收敛到视觉资产准备；未来若要跳过或替换其中步骤，应建模为生命周期或流程规则，而不是混入可选媒体能力目录。

## 账号硬约束

账号蓝图中不可被系列或单条 Episode 覆盖的规则，例如禁止内容、工具权限、素材边界和发布规范。

## 账号默认值

账号蓝图提供的可被系列规则覆盖的默认创作偏好，例如默认语气、视觉风格或字幕样式。

## 系列

账号内可重复使用的内容格式、角色设定和叙事规则。

## 系列基线

某个 Episode 创建时固定引用的系列版本规则，作为该系列内容生成任务的统一上下文。

## Episode

一次独立、可追溯的视频生产单元。创建时固定引用一个账号蓝图版本。

## Episode 生命周期

**归档**：将不再参与默认生产列表的 Episode 隐藏，但保留其数据库记录、本地产物和审计历史。

**永久删除**：由 Owner 明确确认后，删除 Episode 及其任务、产物索引、审计记录和对应的本地 Episode 文件；这是破坏性操作，不是普通的隐藏。

## 产物

生产过程中的可验证输出，例如脚本、视觉资产、分镜、成片、发布包或截图。

## 发布包

经过 QC、可供人工确认发布的一组最终产物：视频、封面、元数据和验证清单。

## 发布记录

一次 Episode 面向具体外部平台的发布事实，至少关联平台、外部地址或内容 ID、实际发布时间和发布结果；同一 Episode 可以拥有多条发布记录。

## 发布确认

Owner 对目标平台已完成人工发布、并已核对发布包内容的明确确认。发布确认是当前的人工控制动作，未来可以由自动发布适配器以同一发布记录模型写入结果。

## 审批

由 Owner 写入、附带结论和理由的状态迁移。生产 Worker 不得审批自己的产物。

## 例外

Owner 对单个 Episode 批准的一次性规则偏离。例外不会自动成为账号蓝图规则。

## 实验

对单个可验证假设的测试，包含一个主要变量、主指标和护栏指标。

## Worker 任务

为 Episode 执行一个可验证生产步骤的后台任务，例如脚本生成、媒体生成、渲染或发布包校验。任务拥有自己的状态、尝试次数、结果和阻塞原因。

## Prompt 上下文

交给生成执行器的结构化创作输入，由账号基线、系列基线、本期输入和审核反馈按任务需要组合而成；技术执行参数不属于创作上下文。
